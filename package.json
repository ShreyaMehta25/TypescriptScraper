{
  "name": "grant-scraper",
  "version": "2.0.0",
  "description": "Monthly grant scraper with Gemini LLM conversion (TypeScript)",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/main.js",
    "dev": "ts-node src/main.ts",
    "scrape-only": "node dist/main.js --scrape-only",
    "convert-only": "node dist/main.js --convert-only"
  },
  "dependencies": {
    "groq-sdk": "^0.12.0",
    "cheerio": "^1.0.0",
    "dotenv": "^16.4.7",
    "playwright": "^1.49.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.0"
  }
}
